{
	"name": "agent",
	"version": "1.0.0",
	"description": "",
	"main": "agent.py",
	"scripts": {
		"expose": "npx localtunnel --port 3001 --subdomain test",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"localtunnel": "^2.0.2"
	}
}
